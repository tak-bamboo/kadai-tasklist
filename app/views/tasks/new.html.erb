<h1>タスク新規登録ページ</h1>

<%= form_with(model: @task, local: true) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.submit 'タスク登録' %>
<% end %>

<%= link_to 'タスク一覧に戻る', tasks_path %>